interface:
  display_name: "Piku App Testing"
  short_description: "Launch and retain isolated Piku test environments"
  default_prompt: "Use $test-piku-app to launch an isolated Piku environment and iteratively test it in the browser while preserving state."
